//Spritesets
spriteset (SPRITESET_TRANSRAPID_PASS, "grf/TRANSRAPID08.png") {
	TEMP_CAR_8_NEW(48,0,0)
}
spritegroup SPRITEGROUP_TRANSRAPID_PASS {
	loading: SPRITESET_TRANSRAPID_PASS;
	loaded:  SPRITESET_TRANSRAPID_PASS;
}

spriteset (SPRITESET_PURCH_TRANSRAPID_PASS, "grf/TRANSRAPID08.png") {
	TEMP_PURCH_SINGLE_NEW(176,14,0,1)
}
spriteset (SPRITESET_PURCH_TRANSRAPID_PASS_COMP, "grf/TRANSRAPID08.png") {
	TEMP_PURCH_SINGLE_NEW_COM(176,14+14*4,0,1)
}

//2cc
spriteset (SPRITESET_TRANSRAPID_PASS_CC, "grf/TRANSRAPID08.png") {
	TEMP_CAR_8_NEW(48+240,0,0)
}
spritegroup SPRITEGROUP_TRANSRAPID_PASS_CC {
	loading: SPRITESET_TRANSRAPID_PASS_CC;
	loaded:  SPRITESET_TRANSRAPID_PASS_CC;
}

spriteset (SPRITESET_PURCH_TRANSRAPID_PASS_CC, "grf/TRANSRAPID08.png") {
	TEMP_PURCH_SINGLE_NEW(176,14+112,0,1)
}
spriteset (SPRITESET_PURCH_TRANSRAPID_PASS_COMP_CC, "grf/TRANSRAPID08.png") {
	TEMP_PURCH_SINGLE_NEW_COM(176,14+14*4+112,0,1)
}

switch(FEAT_TRAINS,SELF,switch_TRANSRAPID_PASS_purchase,PURCHASE){
    1: return SPRITESET_PURCH_TRANSRAPID_PASS_COMP;
       return SPRITESET_PURCH_TRANSRAPID_PASS;
}
switch(FEAT_TRAINS,SELF,switch_TRANSRAPID_PASS_purchase_CC,PURCHASE){
    1: return SPRITESET_PURCH_TRANSRAPID_PASS_COMP_CC;
       return SPRITESET_PURCH_TRANSRAPID_PASS_CC;
}
switch(FEAT_TRAINS,SELF,switch_TRANSRAPID_PASS_purch_CC,PARAM2CC){
    1: return switch_TRANSRAPID_PASS_purchase_CC;
       return switch_TRANSRAPID_PASS_purchase;
}
switch(FEAT_TRAINS,SELF,switch_TRANSRAPID_PASS_CC,PARAM2CC){
    1: return SPRITEGROUP_TRANSRAPID_PASS_CC;
       return SPRITEGROUP_TRANSRAPID_PASS;
}

		//Passenger Car
item(FEAT_TRAINS, TRANSRAPID_PASS, ITEM_TRANSRAPID_PASS) {
	property {
		//Basic
		model_life:	VEHICLE_NEVER_EXPIRES;
		vehicle_life: 45;
		refittable_cargo_classes: bitmask(CC_PASSENGERS);
		cargo_allow_refit: [PASS, TOUR];
		LOADINGSPEED_HIGHSPEED
		running_cost_base: RUNNING_COST_DIESEL;
		misc_flags: bitmask(TRAIN_FLAG_2CC);
		sprite_id: SPRITE_ID_NEW_TRAIN;
		track_type: THREEPHASEHS_MGLV;
		ai_special_flag: AI_FLAG_PASSENGER;
		default_cargo_type: PASS;
		visual_effect_and_powered: visual_effect_and_powered(VISUAL_EFFECT_DISABLE, 0, ENABLE_WAGON_POWER);
		effect_spawn_model_and_powered: EFFECT_SPAWN_MODEL_NONE;
		refit_cost: 0;
		engine_class: ENGINE_CLASS_MAGLEV;
		
		//Trains
		name: string(STR_NAME_TRANSRAPID_PASS);
		REGION(GERMANYHS, CHINA);
		introduction_date:	date(1988,01,01);
		cost_factor: 102;
		running_cost_factor:15;
		power: 0 kW;
		cargo_capacity: 100;
		weight: 27 tons;
		
		length: 8;
		dual_headed: 0;
	}
	graphics {
		default: switch_TRANSRAPID_PASS_CC; // Use this switch to set sprite
		purchase: switch_TRANSRAPID_PASS_purch_CC;
		additional_text: return string(STR_DESC_TRANSRAPID_PASS);
	}
}